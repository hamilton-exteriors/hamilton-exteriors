---
import { Image } from 'astro:assets';
import { financingHouse, financingWorker, iconCheckmarkCircle } from '../lib/images';

interface Props {
  scheduleHref?: string;
}

const { scheduleHref } = Astro.props;
---

<section id="financing" class="w-full py-12 lg:py-[79px] px-0">
  <div class="mx-auto flex max-w-[1294px] flex-col overflow-hidden md:flex-row">
    <!-- Left Column: Photo -->
    <div class="relative h-[280px] w-full md:h-auto md:min-h-[600px] md:w-1/2">
      <Image
        src={financingHouse}
        alt="Hamilton Exteriors financing"
        width={1366}
        height={2048}
        class="absolute inset-0 h-full w-full object-cover"
        loading="lazy"
      />
    </div>

    <!-- Right Column: Content -->
    <div class="flex w-full flex-col justify-center px-5 py-10 md:w-1/2 md:px-[50px] md:py-[80px]">
      <!-- Title -->
      <div class="mb-[20px]">
        <p
          class="font-oswald font-normal uppercase"
          style="font-size: clamp(28px, 5vw, 40px); line-height: 1.2; letter-spacing: 0;"
        >
          <span style="color: var(--color-heading);">Financing Options </span><span style="color: var(--color-primary-dark);">Available For Everyone</span>
        </p>
      </div>

      <!-- Body Text -->
      <p
        class="font-dm-sans text-[0.875rem] leading-[1.5]"
        style="color: var(--color-heading);"
      >
        Flexible financing for every homeowner — high credit or low. We partner with multiple lenders to get you competitive rates. Don't let cost delay your project.
      </p>

      <!-- Assistant Row -->
      <div class="mt-6 flex items-center gap-3">
        <!-- Avatar with green dot -->
        <div class="relative flex-shrink-0">
          <div class="h-[63px] w-[63px] overflow-hidden rounded-full bg-[rgb(13,43,29)]">
            <Image
              src={financingWorker}
              alt="Assistant"
              width={63}
              height={63}
              class="h-full w-full object-cover"
              style="transform: translate(-50%, -50%); position: relative; left: 50%; top: 50%;"
              loading="lazy"
            />
          </div>
          <div
            class="absolute bottom-[2px] right-[2px] h-[12px] w-[12px] rounded-full"
            style="background-color: var(--color-primary-dark); border: 2px solid white;"
          ></div>
        </div>
        <!-- Text -->
        <p
          class="font-dm-sans text-[1rem] leading-[1.5]"
          style="color: var(--color-heading);"
        >
          Let's discuss your financial options. <span class="font-bold" style="font-weight: 700;">Quick Response</span>
        </p>
      </div>

      <!-- Buttons -->
      <div class="mt-6 flex flex-col gap-2.5">
        <!-- Call Now Button -->
        <a
          href="tel:6509773351"
          class="flex w-full items-center justify-center gap-2 rounded-[4px] px-[20px] py-[12px] font-dm-sans text-[0.875rem] font-semibold leading-[1.5] transition-opacity hover:opacity-80"
          style="color: var(--color-heading); border: 1px solid rgba(13, 43, 29, 0.15); background-color: var(--color-green-light);"
        >
          <Image
            src={iconCheckmarkCircle}
            alt=""
            width={15}
            height={15}
            class="h-[15px] w-[15px]"
          />
          Call Now (650) 977-3351
        </a>

        <!-- Schedule Button -->
        {scheduleHref ? (
          <a
            href={scheduleHref}
            class="flex w-full items-center justify-center rounded-[4px] px-[20px] py-[12px] font-dm-sans text-[0.875rem] font-semibold leading-[1.5] text-center transition-opacity hover:opacity-90"
            style="background-color: var(--color-primary); color: rgb(246, 253, 252); text-decoration: none;"
          >
            Schedule a FREE Inspection
          </a>
        ) : (
          <a
            href="#quote"
            class="flex w-full items-center justify-center rounded-[4px] px-[20px] py-[12px] font-dm-sans text-[0.875rem] font-semibold leading-[1.5] text-center transition-opacity hover:opacity-90"
            style="background-color: var(--color-primary); color: rgb(246, 253, 252); text-decoration: none;"
          >
            Schedule a FREE Inspection
          </a>
        )}
      </div>
    </div>
  </div>
</section>

---
import { Image } from 'astro:assets';
import AnnouncementBar from './AnnouncementBar.astro';
import Navbar from './Navbar.astro';
import Hero from './Hero.astro';
import LogoSlider from './LogoSlider.astro';
import Reviews from './Reviews.astro';
import CTA from './CTA.astro';
import Difference from './Difference.astro';
import Financing from './Financing.astro';
import ReviewLogos from './ReviewLogos.astro';
import Projects from './Projects.astro';
import FAQ from './FAQ.astro';
import ContactUs from './ContactUs.astro';
import Footer from './Footer.astro';
import { hasImage } from '../lib/service-page-types';
import type { ServicePageData, SectionBlock, StylesSection } from '../lib/service-page-types';

interface Props {
  data: ServicePageData;
}

const { data } = Astro.props;
---

{data.announcementText ? <AnnouncementBar text={data.announcementText} /> : <AnnouncementBar />}
<Navbar />
<Hero
  headline={data.hero.headline}
  formTitle={data.hero.formTitle}
  formSubtitle={data.hero.formSubtitle}
  ctaText={data.hero.ctaText}
  ctaHref={data.hero.ctaHref}
  heroImage={data.hero.heroImage}
  badges={data.hero.badges}
  serviceType={data.hero.serviceType}
  serviceOptions={data.hero.serviceOptions}
/>

{data.sections.map((section) => {
  if (section.type === 'logoSlider') {
    return <LogoSlider />;
  }

  if (section.type === 'reviews') {
    return <Reviews />;
  }

  if (section.type === 'cta') {
    return <CTA />;
  }

  if (section.type === 'reviewLogos') {
    return <ReviewLogos />;
  }

  if (section.type === 'difference') {
    return <Difference />;
  }

  if (section.type === 'projects') {
    return <Projects showFilters={false} />;
  }

  if (section.type === 'financing') {
    return <Financing />;
  }

  if (section.type === 'faq') {
    return <FAQ />;
  }

  if (section.type === 'contactUs') {
    return <ContactUs />;
  }

  if (section.type === 'footer') {
    return <Footer />;
  }

  if (section.type === 'yellowBar') {
    return (
      <div class="w-full text-center" style="background-color: var(--color-yellow); padding: 14px 0;">
        <a href={section.href} style="font-family: var(--font-dm-sans); font-size: 14px; font-weight: 600; color: var(--color-heading); text-decoration: none;">{section.text} &rarr;</a>
      </div>
    );
  }

  if (section.type === 'imageBanner') {
    const { data: banner } = section;
    return (
      <section class="relative w-full" style={`height: ${banner.height || 400}px;`}>
        <Image
          src={banner.image}
          alt={banner.alt}
          width={1280}
          height={853}
          style="width: 100%; height: 100%; object-fit: cover; display: block;"
          loading="lazy"
        />
      </section>
    );
  }

  if (section.type === 'stats') {
    const { data: stats } = section;
    const padding = stats.desktopOnly ? '60px 0' : '48px 0';
    const gridBreakpoint = stats.desktopOnly ? 'lg' : 'md';
    return (
      <section class={`w-full ${stats.desktopOnly ? 'hidden lg:block' : ''}`} style={`background-color: var(--color-cream); padding: ${padding};`}>
        <div class={`mx-auto max-w-[1200px] px-5 grid grid-cols-1 ${gridBreakpoint === 'lg' ? `lg:grid-cols-${stats.items.length}` : `md:grid-cols-${stats.items.length} md:gap-8`} gap-${gridBreakpoint === 'lg' ? '8' : '4'}`}>
          {stats.items.map((stat) => (
            <div class="text-center">
              <div style="font-family: var(--font-oswald); font-size: 48px; font-weight: 400; color: var(--color-primary-dark); margin-bottom: 8px;">{stat.value}</div>
              <p style="font-family: var(--font-dm-sans); font-size: 14px; color: var(--color-muted);">{stat.label}</p>
            </div>
          ))}
        </div>
      </section>
    );
  }

  if (section.type === 'styles') {
    const styles = section.data;
    const variant = styles.cardVariant;

    /* ─── Roofing variant ─── */
    if (variant === 'roofing') {
      return (
        <section class="w-full" style="padding: 79px 0;">
          <div class="mx-auto" style="max-width: 1182px; padding: 0 15px;">
            <h2
              class="mb-6 text-center"
              style="font-family: var(--font-oswald); font-size: 40px; line-height: 50px; letter-spacing: 0px; color: var(--color-heading); font-weight: 400; text-transform: uppercase;"
            >
              {styles.heading}
            </h2>
            <div class="grid grid-cols-1 gap-5 sm:grid-cols-2" style="margin-top: 20px;">
              {styles.items.map((item) => hasImage(item) && (
                <div>
                  <div class="w-full overflow-hidden" style="">
                    <Image
                      src={item.image}
                      alt={item.title}
                      width={566}
                      height={292}
                      style="width: 100%; height: auto; aspect-ratio: 566 / 292; object-fit: cover; display: block; "
                      loading="lazy"
                    />
                  </div>
                  <div style="padding-top: 16px;">
                    <h3
                      style="font-family: var(--font-oswald); font-size: 18px; font-weight: 700; line-height: 24px; color: var(--color-heading); margin-bottom: 6px; text-transform: uppercase;"
                    >
                      {item.title}
                    </h3>
                    <p style="font-size: 14px; line-height: 150%; color: var(--color-muted);">
                      {item.description}
                    </p>
                  </div>
                </div>
              ))}
            </div>
          </div>
        </section>
      );
    }

    /* ─── Siding variant ─── */
    if (variant === 'siding') {
      return (
        <section class="w-full" style="padding: 79px 0;">
          <div class="mx-auto max-w-[1200px] px-5">
            <h2
              style="font-family: var(--font-oswald); font-size: 40px; line-height: 50px; font-weight: 400; letter-spacing: 0px; color: var(--color-heading); text-transform: uppercase; margin-bottom: 40px;"
            >
              {styles.heading}
            </h2>
            <div class="grid grid-cols-1 gap-5 sm:grid-cols-2">
              {styles.items.map((item) => hasImage(item) && (
                <div>
                  <div class="w-full overflow-hidden" style="">
                    <Image
                      src={item.image}
                      alt={item.title}
                      width={566}
                      height={292}
                      style="width: 100%; height: auto; aspect-ratio: 566 / 292; object-fit: cover; display: block; "
                      loading="lazy"
                    />
                  </div>
                  <div style="padding-top: 16px;">
                    <h3
                      style="font-family: var(--font-oswald); font-size: 18px; font-weight: 700; line-height: 24px; margin-bottom: 6px; color: var(--color-heading);"
                    >
                      {item.title}
                    </h3>
                    <p style="font-size: 14px; line-height: 150%; color: var(--color-muted);">
                      {item.description}
                    </p>
                  </div>
                </div>
              ))}
            </div>
          </div>
        </section>
      );
    }

    /* ─── Windows variant ─── */
    if (variant === 'windows') {
      return (
        <section class="w-full bg-cream">
          <div class="mx-auto max-w-[1294px] px-5 py-20">
            <h2
              class="mb-4 text-center"
              style="font-family: var(--font-oswald); font-size: 40px; line-height: 50px; letter-spacing: 0px; font-weight: 400; color: var(--color-heading);"
            >
              {styles.heading}
            </h2>
            <div class="mt-12 grid grid-cols-1 gap-8 sm:grid-cols-2 lg:grid-cols-3">
              {styles.items.map((item, i) => hasImage(item) && (
                <div class:list={["overflow-hidden rounded-2xl bg-white shadow-sm", i >= 3 && "hidden sm:block"]}>
                  <div class="h-[240px] sm:h-[362px] w-full overflow-hidden">
                    <Image
                      src={item.image}
                      alt={item.title}
                      width={566}
                      height={362}
                      class="h-full w-full object-cover"
                      loading="lazy"
                    />
                  </div>
                  <div class="p-6">
                    <h3
                      class="mb-2"
                      style="font-family: var(--font-oswald); font-size: 18px; font-weight: 800; line-height: 24px;"
                    >
                      {item.title}
                    </h3>
                    <p style="font-size: 14px; line-height: 150%; color: var(--color-muted);">
                      {item.description}
                    </p>
                  </div>
                </div>
              ))}
            </div>
          </div>
        </section>
      );
    }

    /* ─── ADU variant (text-only, no card borders — matches Framer) ─── */
    if (variant === 'adu') {
      return (
        <section class="w-full" style="padding: 79px 0;">
          <div class="mx-auto" style="max-width: 1182px; padding: 0 15px;">
            <h2
              class="mb-6 text-center"
              style="font-family: var(--font-oswald); font-size: 40px; line-height: 50px; letter-spacing: 0px; color: var(--color-heading); font-weight: 400; text-transform: uppercase;"
            >
              {styles.heading}
            </h2>
            <div class="grid grid-cols-1 gap-8 sm:grid-cols-2" style="margin-top: 20px;">
              {styles.items.map((item) => {
                const content = (
                  <div style="padding-top: 4px;">
                    <h3
                      style="font-family: var(--font-oswald); font-size: 18px; font-weight: 800; line-height: 24px; color: var(--color-heading); margin-bottom: 6px; text-transform: uppercase;"
                    >
                      {item.title}
                    </h3>
                    <p style="font-size: 14px; line-height: 150%; color: var(--color-muted);">
                      {item.description}
                    </p>
                  </div>
                );
                return !hasImage(item) && item.href ? (
                  <a href={item.href} class="block">{content}</a>
                ) : (
                  <div>{content}</div>
                );
              })}
            </div>
          </div>
        </section>
      );
    }
  }

  return null;
})}

{data.extraStyles && <style set:html={data.extraStyles} />}
